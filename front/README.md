
  # BookLovers

  This is a code bundle for BookLovers. The original project is available at https://www.figma.com/design/fUaTsZ1JRJSfVjxS6TPVeJ/BookLovers.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  